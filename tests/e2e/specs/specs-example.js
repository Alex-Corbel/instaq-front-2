// Name file example.spec.js

/*var url = 'http://localhost:8080/#/'

describe('Burger Menu Testing', () => {
  beforeEach(() => {
    cy.visit(url)
  })

  it('isBurgerMenuVisible', () => {
    var burgerMenu = cy.get('[class="bm-burger-button"]')
    burgerMenu.should('be.visible')
  })

  it('isBurgerMenuButtonsAreVisible', () => {
    var burgerMenu = cy.get('[class="bm-burger-button"]')
    burgerMenu.click()
    var calculatorSpan = cy.get('[href="#/calculator"]').find('span').contains('Calculatrice')
    var newPharmaSpan = cy.get('[href="#/newstore"]').find('span').contains('Nouvelle Pharmacie')
    var carteSpan = cy.get('[href="#/"]').find('span').contains('Carte')
    carteSpan.should('be.visible')
    newPharmaSpan.should('be.visible')
    calculatorSpan.should('be.visible')
  })

  it('isCalculatorAvailableFromBurgerMenu', () => {
    var burgerMenu = cy.get('[class="bm-burger-button"]')
    burgerMenu.click()
    cy.get('[href="#/calculator"]').click()

    var calculatorView = cy.get('[class="calculator"]')
    calculatorView.should('be.visible')
  })

  it('isNewStoreAvailableFromBurgerMenu', () => {
    var burgerMenu = cy.get('[class="bm-burger-button"]')
    burgerMenu.click()
    cy.get('[href="#/newstore"]').click()

    var newStoreView = cy.get('[class="newstore"]')
    newStoreView.should('be.visible')
  })
})

describe('Map testing', () => {
  beforeEach(() => {
    cy.visit(url)
  })

  it('isInfoWindowVisible', () => {
    var infoWindow = cy.get('[class="infowindow"]')
    infoWindow.should('not.be.visible')
  })

  it('isRedCircleVisible', () => {
    var infoWindow = cy.get('[class="infowindow"]')
    infoWindow.should('not.be.visible')
  })
})
*/
